# Docker Compose 本地开发环境
# 用法：
#   基础服务：docker-compose up -d
#   含 Redis：docker-compose --profile cache up -d
#   仅后端推荐：docker-compose up -d db keycloak + dotnet run

services:
  api:
    build: ./backend
    ports:
      - "5000:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__Default=Host=db;Database=aisch;Username=dev;Password=dev
      - Keycloak__Authority=http://keycloak:8080/realms/aisch
      - Keycloak__RequireHttpsMetadata=false
      - OpenAI__Endpoint=${OPENAI_ENDPOINT}
      - OpenAI__ApiKey=${OPENAI_API_KEY}
      - OpenAI__ModelName=${OPENAI_MODEL_NAME:-gpt-4o}
    depends_on:
      db:
        condition: service_healthy
      keycloak:
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O- http://localhost:8080/healthz || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s

  db:
    image: postgres:18-alpine3.22
    environment:
      POSTGRES_DB: aisch
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "dev", "-d", "aisch"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
    volumes:
      - postgres_data:/var/lib/postgresql/data
      # 首次启动时自动执行补充初始化（扩展、权限等）
      - ./docker/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql:ro

  keycloak:
    image: quay.io/keycloak/keycloak:latest
    command: start-dev
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: admin
    ports:
      - "8080:8080"

  redis:
    image: redis:7.4-alpine3.21
    ports:
      - "6379:6379"
    profiles:
      - cache

volumes:
  postgres_data:
